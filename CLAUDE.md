# CLAUDE.md

Use `AGENTS.md` as the canonical repository instruction file.

For documentation work:

1. read `AGENTS.md`
2. start with `.agents/skills/docs/SKILL.md`
3. route to the relevant skill under `.agents/skills/`

If an **ADR** or **Design Doc** is needed, write and validate it after definition/plan mode and before implementation.
