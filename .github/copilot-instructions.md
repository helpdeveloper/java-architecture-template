# Copilot repository instructions

Use `AGENTS.md` as the canonical repository instruction file for this project.

For documentation-related work:

- start with `.agents/skills/docs/SKILL.md`
- use the artifact-specific skills under `.agents/skills/`
- treat **ADR** and **Design Doc** as pre-implementation artifacts when required

Prefer shared skill files over duplicating instructions here.
